# Automatically Upload Videos to Bilibili

在 Windows 11 上运行的图形化软件：定时把指定文件夹里新出现的视频自动上传到 B 站，支持差异去重、多文件合并为一个稿件（多分P）、大文件自动拆分分P，最大化可自定义。UI 为淡蓝 + 深色主题。

## 用户
不懂代码的初学者。所有面向用户的文档、注释、界面文字一律用中文。操作务必稳定、安全：涉及删除文件、公开发布等不可逆/对外动作，先确认再做。

## 标准文件路径指引

| 文件 | 用途 |
|---|---|
| [docs/开发需求.md](docs/开发需求.md) | 已确认的功能需求与决策（需求基线） |
| [docs/技术设计规范.md](docs/技术设计规范.md) | 技术选型、架构、模块职责、代码与数据规范 |
| [docs/执行步骤.md](docs/执行步骤.md) | 分阶段实施清单，当前进度在此勾选 |
| [开发日志/](开发日志/) | 每天的工作记录（已完成 / 待办） |

## 工作说明

1. 动手写代码前，先读 [docs/执行步骤.md](docs/执行步骤.md) 确认当前阶段，**按阶段逐步推进，不要一口气跨阶段做完**。
2. 实现必须符合 [docs/技术设计规范.md](docs/技术设计规范.md)：技术栈、目录结构、模块划分、命名、数据存储、线程模型、错误处理。
3. 每天工作结束前，在 [开发日志/](开发日志/) 下新建或更新当天日期文件（如 `2026-09-17.md`），记录「已完成」与「待办」。模板见 `开发日志/_模板.md`。
4. 每次会话开始时，先读最新的开发日志和 [docs/执行步骤.md](docs/执行步骤.md)，接着上次进度继续。
5. 代码注释、日志输出、界面文案用中文，与现有代码风格保持一致（类型注解 + 简洁 docstring）。

## 关键决策备忘
- 文件清理默认「移动到归档文件夹」，可切换「永久删除」「仅记录不处理」。
- 登录：扫码 + Cookie 两种都支持。
- 发布默认「延迟发布」，可选「直接发布」或「定时到指定时刻」；「仅自己可见/存草稿」bilibili-api 不支持。
- 多文件默认「一个稿件多分P」（不物理合并）；单个文件过大时物理拆分分P。

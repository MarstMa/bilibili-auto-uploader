# 2026-09-17 开发日志

## 已完成
- 需求沟通 + 技术方案确认（发布方式改用「定时发布」）。
- 建立过程文档：`docs/`、`开发日志/`、`CLAUDE.md`。
- 环境：Python 3.12.10；安装 bilibili-api-python 17.4.2、PySide6 6.11.2、APScheduler 3.11.3；本机已有 ffmpeg 9.0.1。
- **阶段 1 核心模块**（全部自测通过）：config / state / naming / scanner / cleaner / auth / splitter / uploader / scheduler。
- **阶段 2 图形界面**：theme.qss（淡蓝+深色）、main_window（侧边导航 + 托盘）、dashboard、settings、login（扫码+Cookie）、history。离屏与真实窗口启动均正常。
- **阶段 3 集成**：pipeline.py（扫描→命名→拆分→上传→记录→清理）、main.py 入口、「立即运行」+ 定时任务接入。
- 修复扫码登录：B 站改版后 cookie 在 Set-Cookie 头，重写 QrLoginSession（curl_cffi）后登录成功。
- 发布方式扩展为三种：直接发布 / 延迟发布（N 小时）/ 定时到指定时刻（新增 publish_schedule_time + QTimeEdit 时间选择器）。
- 增加自动检查更新：version.py + updater.py（查 GitHub Releases），仪表盘版本显示 + 「检查更新」，启动自动检查 + 托盘提醒。
- 上传 GitHub：仓库 https://github.com/MarstMa/bilibili-auto-uploader（公开，main 分支）。
- 打包：PyInstaller 单文件 exe（内置 ffmpeg/ffprobe），发布 v0.1.0 release（附 BiliAutoUpload.exe）。
- v0.1.1：关键事件（开始上传 / 上传成功 / 上传失败 / 登录失效）通过系统托盘向 Windows 发通知；流程结果增加结构化 event 字段；已重新打包并发布 release。
- 数据目录迁移到 `%APPDATA%/BiliAutoUpload`（C 盘，不再放程序目录），旧 data/ 自动迁移。
- 增加登录自动续期：cookie 过期后用 refresh_token 自动刷新（auth.ensure_valid_credential），流程与启动检查均接入，实现自动登录。
- **0.2.0 大版本**：① 全自动下载安装（询问→下载→update.bat 替换重启）；② 设置项全部加说明 tooltip；③ 仪表盘改版为文件夹卡片（一级）+ 文件夹详情（二级，逐文件夹投稿设置）；④ 配置模型 watch_folders→folders + 每文件夹覆盖（自动迁移）；⑤ pipeline 逐文件夹处理；⑥ 历史页按文件夹筛选。
- **0.2.1 修复**：① 上传状态不明确——last_status_for_folder 路径分隔符不一致（/ 与 \）修复 + 卡片增加「上传中」指示；② 每个文件夹可自定义扫描时间（schedule_times 变为可覆盖，调度器改逐文件夹定时）；③ 新增「启用自动扫描」开关（禁用后跳过定时扫描）。
- **0.2.2 修复**：自动更新卡死——原 update.bat 用 move 覆盖运行中的 exe（被锁定，死循环）。改为「重命名旧 exe → 重命名新 exe → 启动」+ apply_update 后 os._exit 立即退出释放锁。
- **0.2.3**：实现开机自启动（新增 autostart.py，写注册表 HKCU\...\Run，设置页勾选后真正注册/取消）+ 新增「启动时最小化到托盘」选项（start_minimized）。
- **0.2.4**：上传前校验「转载必须有来源」（避免白传）；上传失败日志改一行易读格式；仪表盘新增「扫描」手动刷新按钮；建 experimental 分支 + 实验版本流程文档。
- **0.2.5**：修复定时发布时间过期（大文件上传耗时 > 延迟时长时，发布时间在上传开始时算导致已过期）。改为上传完成、提交前才计算发布时间（VideoUploader 子类）；仪表盘新增「查看日志」按钮。
- 自测脚本：tests/test_core.py、test_auth.py、test_splitter.py、test_gui.py、test_pipeline.py 全部通过。

## 待办 / 下一步
- 等用户实测：GUI 观感、扫码登录、真实上传。
- 阶段 4：PyInstaller 打包 .exe + 捆绑 ffmpeg。
- 阶段 5：端到端验证（多分P上传、大文件拆分、去重、干净环境跑通）。

## 备注
- 踩坑：bilibili-api 需要可选 HTTP 客户端（curl_cffi/httpx/aiohttp 三选一），否则报「尚未安装第三方请求库」。已装 `curl_cffi` 解决，并加进 requirements.txt。
- 踩坑：B 站改版后，扫码登录的 cookie 放在 poll 响应的 `Set-Cookie` 头，而非 `data.url`。bilibili-api 17.4.2 仍按旧方式解析，导致登录后 sessdata 为空（「假成功」）。已重写 `auth.QrLoginSession` 用 curl_cffi 直连接口并从 Set-Cookie 提取 cookie。
- 上传库提交接口 `/x/vu/web/add/v3` 无「仅自己可见/草稿」字段；安全档用 `delay_time`（定时发布）实现。
- 封面必须有效：未指定时自动从首帧抽帧（ffmpeg），失败则 Pillow 生成淡蓝占位图。
- 拆分用 `-c copy` 流复制（按平均码率估算段时长），依赖关键帧，拆分粒度是近似值；阈值可调。
- 归档默认放到各监控文件夹下的 auto_archived，扫描时自动排除该目录避免重复上传。
